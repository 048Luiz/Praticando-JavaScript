//Elaborar um programar que leia um número. Calcule e informe o dobro desse número.

const prompt = require("prompt-sync")();
const number = prompt("Digite um número:");
const result = number * 2;
console.log("Resultado:", result);
